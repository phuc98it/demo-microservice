apiVersion: v1
kind: Service
metadata:
  labels:
    app: reservation
  name: reservation
spec:
  ports:
  - name: "http"
    port: 3000
    protocol: TCP
    targetPort: 3000
  selector:
    app: reservation
  type: NodePort
